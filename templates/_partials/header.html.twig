<header class="sticky top-0 z-40 border-b border-white/10 bg-neutral-950/70 backdrop-blur-xl">
    <div class="mx-auto flex max-w-7xl items-center justify-between px-6 py-4 lg:px-8">
        <div class="flex items-center gap-3">
            <div class="flex h-10 w-10 items-center justify-center rounded-2xl border border-white/10 bg-white/5 text-sm font-medium tracking-tight text-amber-300">
                BM
            </div>
            <div>
                <div class="text-sm font-medium tracking-tight text-white">BannerMods</div>
                <div class="text-xs text-neutral-400">Créer, partager, installer</div>
            </div>
        </div>

        <nav class="hidden items-center gap-8 md:flex">
            <a href="#" class="text-sm text-neutral-300 transition hover:text-white">Découvrir</a>
            <a href="#" class="text-sm text-neutral-300 transition hover:text-white">Collections</a>
            <a href="#" class="text-sm text-neutral-300 transition hover:text-white">Catégories</a>
            <a href="#" class="text-sm text-neutral-300 transition hover:text-white">Guides</a>
            <a href="#" class="text-sm text-neutral-300 transition hover:text-white">Communauté</a>
        </nav>

        <div class="flex items-center gap-3">
            {% if app.user is defined and app.user %}
                <a href="" class="hidden rounded-xl border cursor-pointer border-white/10 bg-white/5 px-4 py-2 text-sm font-medium text-neutral-200 transition hover:bg-white/10 sm:inline-flex">Mon Compte</a>
                <a href="" class="inline-flex cursor-pointer items-center gap-2 rounded-xl bg-amber-400 px-4 py-2 text-sm font-medium text-neutral-950 transition hover:bg-amber-300">
                    <iconify-icon icon="solar:upload-linear" width="18" height="18"></iconify-icon>
                    Publier un mod
                </a>
                {% else %}
                    <a href="{{ path('app_login') }}" class="hidden rounded-xl border cursor-pointer border-white/10 bg-white/5 px-4 py-2 text-sm font-medium text-neutral-200 transition hover:bg-white/10 sm:inline-flex">
                        Se connecter
                    </a>
            {% endif %}


        </div>
    </div>
</header>
